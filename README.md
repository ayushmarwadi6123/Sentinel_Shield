# Sentinel_Shield
A working, runnable teaching WAF/IDS built to satisfy the practical work documentation: it inspects HTTP requests, detects attack signatures, monitors traffic behavior, logs every decision, and visualizes results on a live dashboard.
